import React from 'react'
import { Loader} from "./../components/NavBar/NavStyle";

const Loading = () => {
    return (
        <Loader>
            <p className="loader">Laaodsc</p>
        </Loader>
       )
}
export default Loading